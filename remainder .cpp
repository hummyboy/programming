
#include<stdio.h>
#include<conio.h>
main()
{
	int a,b,remainder;
	printf("enter value of a:");
	scanf("%d",&a);
	printf("entervalue of b:");
	scanf("%d",&b);
	remainder=a%b;
	printf("reminder is %d",remainder);
	getch();
	
}
